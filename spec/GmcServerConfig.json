{
    "mister_host": "192.168.1.255",
    "commands": [
        {
            "cmd": "mame",
            "work_dir": "/path/containing/groovymame",
            "exec_bin": "./mame",
            "exec_args": ["${MACHINE_NAME}"]
        }
    ]
}